package model;

public class Professor extends Pessoa{

	//Call the extended variables from the class "Pessoa".
	public Professor(String nome, int idade, String cargo) {
		super(nome, idade, cargo);
	}
}
